node
   {
   stage "1. update the system"
   sh "yum update -y"

   stage "2. install the package"
   sh "yum install -y git"

   stage "3. create folder"
   sh "mkdir scripted-pipeline3"
   
   stage "4. cd scripted-pipelne"
   sh "echo test data >> testfiles-1"
   }
